# Antigravity Bus Bridge

This extension is the companion runtime bridge for `antigravity-bus`.

It watches a local inbox directory, executes Antigravity runtime commands such as `antigravity.sendPromptToAgentPanel`, and writes machine-readable results into a local outbox.

The default bridge directory is:

`~/Library/Application Support/Antigravity/antigravity-bus-bridge`
